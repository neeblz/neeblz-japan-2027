# Япония 2027 — страница поездки

Одна страница: сверху нитка всей поездки, под ней карточки городов, в них —
брони, сроки отмены и места, куда Ни хочет сходить. Живёт за паролем,
собирается из одного файла с данными.

Так её выбрала Ни 23 августа 2026: «я бы скрестила нитку с городами. сверху
нитка и потом карточки. с местами». Про прежнюю длинную версию она сказала
«слишком много листать вниз» — поэтому списки, дни и багаж не выкинуты, а
свёрнуты внизу под три строки.

| | |
|---|---|
| Адрес | **https://japan-2027-eti.pages.dev** |
| Пароль | в `../.japan-password` на этой машине, в git его нет |
| Хостинг | Cloudflare Pages, проект `japan-2027` |
| Данные | `data/trip.json` — брони и деньги · `data/days-plan.json` — засев плана по дням |
| Сборка | `python3 build.py` → `dist/` |
| Выкладка | `./deploy.sh` |
| Хранилище | привязка `JAPAN_KV` в панели Cloudflare Pages — её записи (`japan.v1`) и её порядок дней (`japan.days.v1`) живут там |
| Тесты | `npm test` (20 · дверь, 19 · записи и чек, 23 · порядок дней) · `python3 test/test_data.py` (107 · данные и страница) |
| Линейка | `../.venv-shot/bin/python3 test/wide.py` (собранное, 1440 и 390, контраст) |
| Круг | `./test/round.sh` (26 · запись и расстановка переживают пересборку, на настоящей KV) |
| Руками | `./test/by-hand.sh` (49 · её путь в браузере, поднимает сайт сам) |
| Живая проверка | `../.venv-shot/bin/python3 test/look.py` (выложенное, телефонная ширина) |

Это отдельный проект. Он не трогает ни курс лекций, ни вишлист: свой каталог,
свой проект в Cloudflare, свой пароль, свои функции.

## Как внести правку

Всё содержимое страницы — в `data/trip.json`. Ни числа, ни даты, ни суммы
руками в вёрстку не пишутся.

```
python3 build.py     # проверит и соберёт
./deploy.sh          # выложит, если дверь на месте
```

`build.py --check` прогоняет только проверки, ничего не собирая.

Что где лежит в `trip.json`:

| Раздел | Что правит |
|---|---|
| `trip` | заголовок, даты, дата обновления в подвале |
| `route` | порядок городов; сама нитка рисуется из `stays` |
| `stays` | карточки городов и отрезки нитки; ночи, суммы и сроки — отсюда |
| `places` | места из вишлиста внутри карточки города — желания, не брони |
| `unknown` | пустые поля с подписью: чего мы ещё не знаем |
| `transfers` | переезды между городами: поезд, время, цена — рисуются в нитке |
| `not_in_total` | чего нет в итоге под чеком; без чисел, строка стоит под суммой |
| `cancelled` | отменённые брони — показываются зачёркнутыми, в суммы не идут |
| `transit` | ночи без отеля — вылет 4 января |
| `alerts` | ночь с двумя бронями; `level`: `red` — блок на странице, `calm` — только в данных |
| `todo` | чеклист «решить и забронировать» (свёрнут внизу) |
| `days` | планы на конкретный день (свёрнуто); города подставляются сами |
| `luggage` | чемодан целиком: служба, ссылка, цена, переезды (свёрнут внизу) |
| `notes` | «сверх этого — считается на месте», рядом с деньгами |

**Соседние брони в одном месте сливаются в один город.** Две ночи в Киносаки
куплены двумя бронями — на нитке и в карточке это один отрезок и одна цена, но
сроки бесплатной отмены у них разные, поэтому внутри карточки каждая бронь
остаётся своей строкой со своим сроком. Слить их значило бы показать один срок
вместо двух.

**Оплаченное и прожитое — разные вещи.** Отрезок рисуется по тем ночам, когда
она там спит (`arriving`), а оплаченное сверх этого показано тонкой полосой под
отрезком. Поэтому OMO3 в нитке начинается 15-го, а полоса под ним — с 14-го.

**Снаружи карточки — то, что решает и стоит денег.** Город, даты, цена, срок
бесплатной отмены и «хочу сходить». Комната, еда, часы заезда, адрес и телефон
живут под стрелкой «подробности проживания» — её слово 23 августа: «подробности
о проживании давайте сворачивать под стрелку». Прежнее «ещё про отель» слито
туда же: две стрелки подряд в одной карточке — это не выбор, а лишнее нажатие.
Заголовок перечисляет, что внутри; свёрнутое без подписи превращается в вопрос
«а надо ли туда лезть», который приходится решать нажатием.

**Место из вишлиста висит на броне** (`"stay": "lyf"`), а не на городе: Токио в
поездке дважды, и матча-бар в Гиндзе нужен в первой половине, а лавка у
Асакусы — во второй. Опечатка в `stay` останавливает сборку: иначе место просто
тихо не появится. Первые четыре места в городе видны сразу, остальные — под
строкой «ещё N мест», чтобы десятое место не растянуло карточку.

Даты — только `ГГГГ-ММ-ДД`. Сроки отмены — `ГГГГ-ММ-ДДTЧЧ:ММ`, всегда по JST:
страница сама подписывает «JST» и считает, сколько осталось.

## Сроки отмены и переезды

**Ближайший срок бесплатной отмены стоит в шапке**, остальные — под стрелкой
рядом. Это самые дорогие даты страницы: после них деньги не возвращают вовсе, а
лежали они по одной внутри четырёх карточек, и половина карточки к тому же
уехала под стрелку «подробности проживания». Чтобы узнать, что горит ближайшим,
приходилось сравнивать четыре даты в четырёх местах.

Место выбрано не только по важности: правый столбец шапки ниже заголовка, и
блок встал в уже потраченную высоту — вся страница подросла на две точки.

**Дни считает браузер, а не сборка.** В разметку уезжает только дата: она не
стареет, а «осталось 116 дней» стареет каждые сутки, и страница живёт неделями
между сборками. Считается по японскому календарю целыми сутками — раньше здесь
стоял `Math.ceil` от разницы мгновений и давал лишний день (24 августа до
18 декабря 116 дней, а страница говорила 117). Ошибка была в её пользу, то есть
в ту сторону, в которую на денежном сроке ошибаться нельзя.

Ближайший выбран при сборке и назван вслух в её выводе. Если он всё-таки успел
пройти раньше следующей пересборки, браузер зачёркивает его и раскрывает список
сам, чтобы живые сроки не остались спрятаны за мёртвым. Проверено переводом
часов браузера, а не рассуждением.

**Переезды стоят в нитке**, между отрезками: поезд, время в пути и цена. Раньше
поезда лежали только пунктами то-до — то-до говорит «купить билет», а нитка
обязана говорить «чем и сколько ехать». На телефоне числовая ось и стрелки
«прилёт / домой» прячутся, а переезды остаются: в дороге они нужнее всего.

**Чего не знаем — пустое поле с подписью**, не прочерк и не правдоподобное
число: прочерк читается как «бесплатно», выдуманная цифра — как подтверждённая.
Цены экспресса Киото → Киносаки у нас нет. У обратного пути 15 января известна
половина — ¥13 970 за синкансэн, — и рядом с числом стоит пустое поле с
подписью «экспресс до Киото»: одно число без него прочиталось бы как цена
всего переезда.

**Под итогом сказано, чего в нём нет.** Крупная цифра сама себя объявляет
полной стоимостью поездки, и подписи «жильё» над ней хватает ровно до тех пор,
пока её читают. Строка стоит между суммой и разбивкой оплаты — в одном взгляде
с числом — и перечисляет без цифр: поезда, пересылка чемодана, еда и входы,
метро, сувениры. Список в `not_in_total`; всё, что переезжает в чек, из него
вычёркивается, иначе строка начнёт врать в другую сторону. Перелёт так и
вычеркнулся 24 августа — вместе с настоящим билетом (см. ниже).

## Перелёт

Настоящий билет Turkish Airlines, `flights` в `trip.json`. Два плеча по два
рейса: 4 января Тбилиси → Стамбул → Нарита, 19-го Ханэда → Стамбул → Тбилиси.

**В нитке он стоит первой и последней меткой.** До этого линия начиналась прямо
с города — она возникала в Токио, — а домой уезжала в пустоту за правым краем.
Плечо названо аэропортами, а не городом, потому что аэропорты разные: прилёт в
Нариту, вылет из Ханэды. В последний день это не буква, а час дороги из
Асакусы, поэтому читаться должно без раскрытия — на нитке и на последнем
столбце («19 · домой · Ханэда 22:05»).

**Подробности — под стрелкой**, тремя карточками в ряд: туда, обратно, билет.
Снаружи путь и часы, внутри номера рейсов, самолёты, пересадка между ними,
багаж и правила обмена. Ряд, а не стопка: три полосы во всю ширину — это три
экрана прокрутки за тем, что читается одной строкой каждое.

**Срока бесплатной отмены у билета нет вовсе**, и потому наверх, к отельным
срокам, перелёт не идёт: выдуманная дата там была бы худшим видом подсказки.
Сказано это словом там же, где показаны остальные правила, — «бесплатной
отмены нет: срока, до которого деньги вернут, у билета не существует».

**Денег в этом блоке нет ни одной цифры, и это не забывчивость.** Билет уже
куплен и уже стоит в чеке — её записью, $1 222,10, «оплачено». Сборка его не
складывает и сложить не может: она считает жильё. Зато она умеет сказать про
него второй раз словом — «ещё не посчитано» или «в это число не входит», — и
тогда одна и та же тысяча долларов оказывается на странице дважды и с
противоположными знаками. Ровно это и сторожит правило 17 в `check()`, а
`test_data.py` печатает цену билета в обеих валютах и требует, чтобы её на
собранной странице не нашлось.

Три примечания из `flights.notes` стоят открытыми: они не про билет, а про
планирование — какой аэропорт ближе, чем занят последний день, где оформлять
такс-фри.

## Что вносит она сама

Страница перестала быть только чтением 23 августа: «сделайте так, чтобы я могла
вносить места как-то, некоторые будут заранее купленными бронированиями они тоже
должны вноситься в общий чек».

Три вида записей, у каждой цена (или честная пустота) и состояние денег —
`уже оплачено` / `предстоит` / `плачу на месте`, те же три, что у броней отеля:

| Вид | Куда ложится | По умолчанию |
|---|---|---|
| **место** — «хочу сходить» | в карточку выбранного города, рядом с местами из `trip.json` | плачу на месте |
| **бронь** — билет, поезд, экскурсия | раздел «Куплено отдельно» | уже оплачено |
| **пункт то-до** — виза, багаж, дьюти-фри, регистрация | раздел «Решить и забронировать», в выбранную группу | предстоит |

Цена необязательна везде: «виза» бывает просто галочкой. **Пустая цена — не
ноль**: она не двигает итог, а страница называет вслух, сколько записей без
цены. Ноль значит «бесплатно», а не «не знаю».

Цену можно вписать в иенах или в долларах. Введённое в долларах остаётся её
числом ($12 не превращается в $13 пересчётом туда-обратно), а в сумму идёт
пересчитанным по курсу из `trip.json`.

**Где это лежит.** Не в `dist/` и не в git, а в Cloudflare KV — привязка
`JAPAN_KV`, одно значение под ключом `japan.v1`: её записи и её галочки.
Причина простая: `build.py` стирает `dist/` целиком, и запись, попавшая в
собранную папку, исчезла бы при первой же выкладке. Это проверяется настоящим
кругом (`test/round.sh`): внесли → `dist/` стёрт и собран заново → запись на
месте.

**Ручка одна** — `/api/entries`, GET/POST/PATCH/DELETE. Своего пароля у неё нет
нарочно: `_middleware.js` стоит перед каждым запросом, и второй замок на той же
двери — это второе место, где его можно забыть запереть. Без печенья ручка
отвечает 401, без привязки `JAPAN_KV` — 503 словами «не сохранилось, скажи
Блэйзу», а страница показывает эти слова вместо того, чтобы проглотить
набранное.

**Галочки переехали из телефона в хранилище.** Раньше они жили в
`localStorage`, то есть терялись при смене устройства; список, который забывает
отмеченное, — это список, которому нельзя доверить визу. Хранится по-прежнему
**отличие** от `trip.json`, а не состояние: когда решение переедет в файл,
галочка не начнёт спорить сама с собой.

**Правила, которые ручка держит вместо нас.** Место обязано висеть на
существующем городе (`_stays.js` собирается `build.py` и совпадает с мешками на
странице — принятая запись обязана иметь, куда показаться); запись без названия,
отрицательная цена, несуществующая дата и чужое слово в состоянии оплаты
отбиваются с объяснением, а не молча.

## Дни: её порядок, а не файловый

Её слово 24 августа: «вот это забери расписание. но мне нужно сделать так, чтобы
можно было места тасовать и переносить из дня в день. и ссылки на них нужны».

Расписание — шестнадцать дней, 4–19 января, 98 пунктов. Её текст разобран в
`data/days-plan.json` **как есть** и оттуда не переписывается «покрасивее».

**Файл — засев, а не хозяин.** Хозяин порядку — она:

- пусто в хранилище → показывается файловый порядок;
- она что-то сдвинула → появляется ключ `japan.days.v1`, и с этого момента
  пересборка порядок не трогает;
- id пунктов (`d07-2`) вечные — по ним хранилище узнаёт передвинутое. **Не
  перенумеровывать даже при правке текста**: поправка в файле доезжает до неё
  именно по id, а сменившийся id читается как новый пункт.

В хранилище лежит только расстановка: `order` (дата → id по порядку), `own` (её
собственные пункты), `edits` (её текст поверх файлового), `gone` (убранное —
без этого мешка каждая пересборка воскрешала бы удалённое). Текст пунктов
остаётся в файле, поэтому опечатку правит Блэйз, и она видит поправку, не теряя
своих перестановок. Проверено настоящим кругом (`test/round.sh`): передвинула →
`dist/` стёрт и собран заново → пункт на новом месте; а пункт, дописанный в файл
после этого, встаёт на своё файловое место, не сбив её порядок.

**Двигает она мышью — и только мышью.**

- **Внутри дня и между открытыми днями** — перетаскиванием. Место считается по
  середине строки под курсором: выше середины — перед ней, ниже — за ней.
- **В другой день, хоть с 6 января на 17-е** — броском на **заголовок
  свёрнутого дня**: пункт встаёт в его конец, а день сам открывается, чтобы
  было видно, куда он лёг. Под грузом заголовок подсвечивается — песком и
  пунктиром, тем же языком, что и открытый день.

Список «в день →» (шестнадцать дат у каждой строки) убран 24 августа. Он
существовал ровно потому, что дни стояли колбасой вниз и до 17 января мышью
было не дотянуться; её ответ на это: «поэтому я попросила сделать дни в два
столбца, а не колбасой вниз». В два столбца все шестнадцать свёрнутых дней
занимают **875 точек** (879 в прогоне `test/wide.py`, где раскрыто и всё
остальное: полоса прокрутки сужает страницу и добавляет перенос) и помещаются
на экран в 900 целиком. Между заголовками 6 и 17 января 482 точки. Меряется на
каждом прогоне: перерастёт экран — и дальний перенос станет нечем делать.

Стрелок «↑ / ↓» тоже нет (24 августа): «все кнопки убивай, они только
мусорность создают и место занимают» — перетаскивание она освоила, а строка
вверх-вниз им и делается.

**Чего теперь нельзя.** Перенести пункт в другой день **с клавиатуры** — список
дат был единственным таким способом, а у перетаскивания клавиатурной пары нет.
Перенести пункт **с телефона** — там нет ни перетаскивания, ни списка; правка,
«+ пункт» и крестик работают, порядок собирается с компьютера. Обе потери
названы, а не забыты: вернуть их можно, но это отдельная вещь и её решение.

**Ручки спрятаны до наведения.** В спокойном виде строка — это только текст;
«правка» и крестик появляются под мышью и под фокусом клавиатуры. Спрятаны они
прозрачностью, а не `display:none`: тот выкидывает кнопку из обхода
клавиатурой, и «спрятано от мыши» молча стало бы «недостижимо без мыши». На
телефоне ручки видны всегда — наведения там не бывает.

Плюс «+ пункт в этот день», «правка» и «убрать» (теперь просто ✕, с подписью
для голоса и клавиатуры). Удаление спрашивает
подтверждение, и «нет» слушают. У пункта **из плана** пустое название в форме
значит «вернуть как в плане» — это единственный способ снять свою правку, и
поэтому поле не помечено обязательным; у её собственного пункта пустое название
отбивает хранилище словами.

**Ручек нет, пока хранилище не ответило.** Собранная страница — это план из
файла со ссылками и строка о том, почему двигать его сейчас нечем. Кнопка,
которой некуда нажать, — обещание, которого страница не может сдержать.

**Ручка** — `/api/days`, GET/POST/PATCH/DELETE, за той же дверью и с тем же
503 при отсутствии привязки. Ключ отдельный от записей нарочно: перетаскивание
пункта не должно переписывать её записи целиком. План для двери выкладывает
сборка (`_plan.js`) — только id, без текста: второй экземпляр текста рядом с
хранилищем означал бы, что поправка доезжает до неё через раз.

**Ссылки — по одной на место.** Название ведёт **на карту**, а не на сайт: в
поездке от названия нужно «где это». У «обеда» и «выезда» адреса нет, и они
ссылками не притворяются.

Строка, называющая несколько мест, разрезана: её слово 24 августа —
«залинковывай **каждую локацию отдельно**, а не строку целиком, чтобы я не
гадала, что же по ссылке откроется». «Yasaka Shrine, Maruyama Park, Chion-in»
было одной ссылкой на три места и открывалось на первом. Разбивка лежит в
данных полем `spots`, и сборка падает, если место из него не находится в самом
названии или стоит там не по порядку: ссылка вешается на кусок текста, и
несовпадение иначе просто оставило бы место без ссылки — молча.

Всего в названиях 67 ссылок: 38 целых названий и 29 мест внутри разрезанных
строк. Из них 64 ведут на карту, три — на сайт места, проверенного в `places`
(KUMONOCHA дважды и AGE.3): карты у них нет, а сайт есть. Куда ведёт ссылка,
написано на ней самой (`title`) — снаружи карта и сайт выглядят одинаково.

**Адрес бронируемого уехал в само слово «бронировать».** Её слово: «там, где
надо бронировать проставь ссылки на сайт прямо в надписи бронировать, **не
придумывай новую**». Отдельной метки «сайт» больше нет. Ссылками стали пять
пометок из девяти — Shibuya Sky, teamLab Planets, teamLab Borderless и два
стола из `places`; у поездов и чемодана своей страницы нет, и пометка там
осталась текстом.

Побочный итог, который стоит держать в голове: **сайт Tokyo National Museum со
страницы ушёл**. Он был проверен и стоял меткой «сайт», но бронировать музей не
надо, и места для адреса у него теперь нет. Вернуть можно тремя способами —
пометкой `book`, разбивкой `spots` или чем-то новым; последнее она запретила.

**Одна цифра — одно место.** Пункт-переезд говорит «переезд · время и цена в
«Переездах»» и не носит второго числа; чемодан живёт в «Багаже», а в дне
остаётся только её примечание к дню; брони жилья в днях не повторяются. Цен у
пунктов нет вовсе, и в общий чек дни не идут.

**Часов на строках нет.** Её слово 24 августа: «часы убей, они ломаются при
перетаскивании и в целом лишние, **не хочу жить по расписанию**. а если где-то
важно время — пометь, что к примеру только до 15». Поле `time` из данных не
выброшено — её текст мы не выкидываем, — оно просто не рисуется; вместо него
`when`: короткая пометка там, где время действительно связывает (заезд, выезд,
слот, расписание поезда). Таких 17 на 98 пунктов, у остальных не рисуется
ничего. Часы работы музеев и храмов в пометки не пошли: мы их не проверяли, а
пометка времени с потолка хуже отсутствующей.

**Свёрнуто по умолчанию**: раскрыт отрезок города, свёрнут день, ближайший день
раскрывает браузер (он же и считает, какой ближайший — «ближайший» стареет
каждые сутки, а страница собирается редко). Одна кнопка на весь раздел
разворачивает и сворачивает все дни разом; она стоит в собранной разметке, а не
рисуется скриптом, потому что свёртка ни в какое хранилище не ходит и работает
даже тогда, когда расстановка не доехала.

**Дни в два столбца** — её слово «сделай дни в два столбца». Уже на 820 точках
ширины и шире; на телефоне столбец один. Высоты страницы раздел не касается (он
внутри свёрнутого), но внутри стал вдвое короче: со свёрнутыми днями 1336 → 981
точка, со всеми развёрнутыми 7035 → 5079.

Подписи «16 дней · 98 пунктов» у свёртки больше нет — «подпись к блоку и все
кнопки убивай, они только мусорность создают и место занимают». Счёт пунктов
остался там, где он что-то значит: на самом дне.

## Чек: жильё или вся поездка

Итог под городами — одно место, которое умеет говорить о двух разных числах, и
**подпись всегда про то число, которое рядом**:

- записей нет или они не доехали — заголовок «жильё», число `¥290 675`, и
  строка под ним говорит, почему;
- записи есть — заголовок «вся поездка», число включает её записи, и под ним
  появляется разбивка: жильё · брони · места · то-до.

Сходится это двумя столбиками сразу, и оба видны на экране: **сумма разделов =
итог** и **оплачено + предстоит + на месте = итог**. Проверяется тестом
(`entries.test.mjs`) и вторым разом в браузере на живых числах (`hands.py`) —
цифра, которую нельзя проверить глазами, плохая цифра.

Доллар итога — по-прежнему сумма показанных долларов, а не пересчёт общей иены
(см. ниже). Курс — одной константой в `trip.json`, подписан человеческой датой.

## Линейка: иены в доллары и обратно

Её слово 24 августа: «мне нужен конвертер из йен в доллары и обратно.
небольшой, где-нибудь». Два поля, знак между ними, подпись под ними — и всё:
ни кнопки «посчитать», ни истории, ни второй валюты. Вписала — увидела.

**Своего курса у линейки нет.** Числа нет ни в разметке, ни в скрипте: браузер
берёт его из того же `#japan-data`, из которого чек складывает её записи, и
считает тем же `money.js` — иена в доллар делением, доллар в иену умножением.
Второй курс или вторая арифметика на одной странице — это два числа с одним
именем, и разойтись они успеют молча. Напечатан курс дважды — под чеком и под
полями, — но оба раза сборкой из одного `fx`.

**Место выбрано по пустоте, а не по смыслу.** Средний столбец чека («сверх
этого — считается на месте») был на 137 точек ниже левого, и линейка встала
целиком в эту пустоту: страница не выросла ни на точку — 1803 до и после. Под
суммой она вытолкнула бы её за потолок. `wide.py` сторожит именно это:
средний столбец обязан остаться не выше левого.

**Ничего не сохраняется и никуда не ходит.** Набранное живёт до перезагрузки:
это счётная линейка, а не запись. Поэтому её код лежит в общем скрипте, а не в
«её странице» — считать она обязана и при мёртвом хранилище.

## Багаж — в одном месте

Её слово 24 августа: «мы в трёх местах пишем про багаж и нигде не указываем
сайт, откуда вызывать доставку… надо собрать информацию так, чтобы она не
дублировалась». Три места — то-до, подробности OMO3 и блок «Багаж» — держали по
половине «как», и ни одно не называло главного: кто везёт, куда нажимать и
почём.

Теперь всё в `luggage`: **Yamato Transport, услуга TA-Q-BIN** со ссылкой,
цена ¥4 600 — 5 260 за обе пересылки, размер 140/160 и два переезда чемодана.
В то-до остались только галочки, в подробностях OMO3 — строка «смотри в
«Багаже»» вместо копии.

**Первое, что говорит блок, — что нажимать негде.** TA-Q-BIN заказывается на
стойке отеля, а не через интернет; без этой строки ссылка обещала бы кнопку
заказа, которой там нет. Ссылка ведёт не на главную компании — та называет три
вида бизнеса и ни одного слова про чемодан гостя, — а на страницу самой
услуги, открытую гостевым браузером: 200, заголовок «TA-Q-BIN | YAMATO
TRANSPORT».

**Имя службы и цена стоят в подписи свёрнутой стрелки**, а не только внутри:
свёрнутое без подписи превращается в вопрос «а есть ли там что-нибудь»,
который приходится решать нажатием.

## Что проверяет сборка

`build.py` не соберёт страницу, если данные не сходятся. Каждая проверка
существует потому, что ошибка в ней стоит денег:

1. **Ночи против календаря.** `nights` обязано совпасть с разницей дат.
2. **Ночь без крыши.** Каждая ночь поездки чем-то закрыта — иначе видно сразу.
3. **Наложение нельзя потерять.** Две брони на одну ночь обязаны быть помечены
   `"conflict"` и объявлены в `alerts`. И наоборот: блок, под которым наложения
   уже нет, — тоже ошибка сборки. Тихо убрать его не выйдет ни в ту, ни в другую
   сторону.

   Поле `level` задаёт, **показывать ли блок вообще**: `red` — «нужно решение»
   (киноварь, варианты со сроками, счётчик в шапке), `calm` — на странице
   ничего. Ночь 14 → 15 января стоит как `calm`: Ни решила, что спит в
   Киносаки, а оплаченная ночь в OMO3 остаётся пустой.

   Спокойный блок рисовался до 24 августа и был убран её словами: «блок **как
   задумано** убивай, он действует на нервы и мешается» — страница повторяла
   ей решённое на первом экране при каждом заходе. Убран показ, а не данные:
   правило выше держится ровно так же, и наложение по-прежнему нельзя потерять
   молча.
4. **Ночь без отеля** — только объявленная в `transit` (например, 4 января в
   самолёте). Молча пропущенная ночь по-прежнему останавливает сборку.
5. **Деньги.** Сумма всех броней = списано + спишется + оплата на месте.
6. **Место, висящее на несуществующей брони.** Опечатка в `places[].stay` —
   это место, которое молча не покажется: страница соберётся, а его на ней
   не будет.
7. **Переезд на дне, когда никто никуда не едет.** Дата в `transfers` обязана
   совпасть с днём переезда из броней, а `from`/`to` — с настоящими городами.
   Дата мимо — переезд, который тихо не покажется в нитке; города мимо —
   подпись, которая врёт рядом с верной картинкой.
8. **Число в строке «чего нет в итоге».** `not_in_total` — перечисление без
   цифр: цифра здесь была бы вторым счётом рядом с первым.
9. **Служба доставки названа один раз.** Слово «Yamato», «TA-Q-BIN» или
   «宅急便», встреченное где-нибудь кроме `luggage.service`, останавливает
   сборку; ссылка без `https://` или без подписи — тоже. Собрать про багаж
   один блок — работа на полчаса, удержать его собранным — работа навсегда:
   дублируется не слово, а цена и условия, расходятся они молча, а замечаются
   на стойке в чужой стране.
10. **Просроченные сроки отмены** называются вслух при сборке, и вслух же
   называется ближайший — тот, что стоит в шапке страницы.
11. **Досмотр на секреты.** Почта, номер карты, слова «пароль», «PIN», «номер
   брони» в данных — сборка останавливается. Досматриваются оба файла: план по
   дням такой же git-файл и такая же страница. Git не забывает.
12. **Раздел `days` в `trip.json`.** Его там больше нет и завести заново нельзя:
   план по дням живёт в `days-plan.json`, а порядок пунктов — в хранилище. Два
   заголовка на одну дату в двух файлах расходятся молча.
13. **Дни плана против дней поездки.** Ровно те же даты, ровно в том же
   порядке: день мимо поездки — пункты, которых она не найдёт нигде.
14. **Неповторимые id пунктов.** Два пункта с одним id ходят парой и удаляются
   вместе — молча, потому что хранилище различает их только по id.
15. **Ссылки и отсылки в плане.** `site` — только `https://`; `place` — только
   имя, которое правда есть в `places` (и не вместе со своим `site`: адрес
   живёт в одном месте); `transfer` — только на дне, когда по броням правда
   едут. Плюс сверка с её собственным списком «что обязательно бронировать»:
   пункт, потерявший id при разборе, иначе исчез бы молча.
16. **Города плана против городов броней.** Раздел дней и нитка обязаны
   рассказывать один маршрут — иначе деньги считались бы по одному, а дни
   читались по другому.
17. **Перелёт посчитан один раз.** Плечи стоят на днях начала и конца поездки,
   у каждого есть рейсы, и слово «перелёт» не встречается ни в «ещё не
   посчитано», ни в строке «чего в итоге нет»: билет куплен и стоит в чеке её
   записью, а те два места сказали бы обратное. Появившаяся у билета бесплатная
   отмена тоже останавливает сборку — значит, у него появился срок, и место
   срока наверху, вместе с отельными, а не словом внизу.

На каждое из этих правил в `test/test_data.py` есть нарочно испорченная копия
данных, которую оно обязано поймать. Проверка, которая ни разу не падала, и
отсутствие проверки выглядят снаружи одинаково.

## Светлый фон и читаемость

**Страница всегда светлая.** Тёмная тема в ней была и включалась сама, по
настройке телефона, — именно её Ни и увидела 23 августа словами «а сделайте на
светлом фоне, а то сложно читать». Поэтому `prefers-color-scheme` из стиля
убран, а рядом стоит `color-scheme:light`: без него светлыми останутся только
наши цвета, а поля ввода и полосу прокрутки браузер всё равно нарисует
тёмными.

Проверять это глазами бесполезно — на нашей машине светло всегда. `wide.py`
открывает страницу браузером с **включённым тёмным режимом** и меряет
настоящий цвет фона.

**Контраст — число, а не вкус.** Пороги её: основной текст не ниже **7:1**,
подписи и мелочь не ниже **4.5:1**. `wide.py` пробегает каждую видимую строку,
считает цвет текста поверх того фона, который под ним реально оказался, и
называет просевшие поимённо. Считать по переменным в `:root` мало: `opacity`
на предке превращает записанные 5.6:1 в 2.9:1, а переменная об этом не знает.

Так нашлась настоящая причина «сложно читать»: серый `--quiet` держал 3.3:1 и
нёс на себе почти всю мелочь — 180 строк из измеренных. Каждый цвет в `:root`
теперь подписан своим контрастом к бумаге.

**Что различимо без цвета.** Плохой экран первым съедает оттенок, поэтому
смысл нигде не держится на нём одном:

- три доли оплаты в полосе — заливкой и направлением штриховки, а в квадратиках
  у сумм, где на фактуру нет места, — наполненностью: полный, половина, пустой.
  Плотность у трёх полос одинаковая нарочно: лесенка «густо → редко» рисовала
  бы самую крупную сумму самой бледной, а «на месте» — крупнейшая доля;
- «оплачено сверх прожитого» в нитке — обводка и косая штриховка, а не тот же
  цвет побледнее;
- помеченная бронь — толщиной полоски слева, а объясняет себя словами внутри;
- «хочу сходить» — пунктирная граница и ромбы против сплошных линий брони.

## Сколько страница занимает

`test/wide.py` меряет **собранный** `dist/index.html` браузером: высота при
ширине 1440 — не больше **1980 точек**, сейчас 1979. Порог всегда ставится по
последнему измерению, а не по запасу: сетка, сквозь которую свободно пролезает
двести точек нового, ничего не ловит. Потолок «2 экрана это ок» (1800) — её
слово 23 августа, и он превышен: 1979 это 2.2 экрана. Разговор про это с ней, а
не с сеткой — подгонять вёрстку под число, которое мы сами назначили, значит
прятать вопрос вместо того, чтобы его задать.

Последнее крупное — перелёт, 107 точек 24 августа: два плеча, билет и три
примечания. Вернуть 88 можно, поставив четыре свёртки над справкой в два
столбца (сейчас каждая держит одну строку текста в 1308 точках ширины), но это
правка чужого раздела и решает её Ни.

История порога — история страницы: 1800 у длинной версии, 1650 после того как
подробности проживания уехали под стрелку (1604), 1780 когда появились кнопки
«вносить своё», раздел «куплено отдельно» и разбивка чека (1736), 1810 сейчас
(1803) — сроки в шапке, переезды в нитке, строка «чего в итоге нет» и вилка
цены у экспресса.

Линейка и собранный багаж 24 августа не стоили ни точки: линейка встала в
пустоту среднего столбца чека, а багаж лежит внутри свёрнутой стрелки, у
которой высоты нет. Запас до потолка от этого не вырос — он всё те же семь
точек, и следующее, что полезет наружу, придётся чем-то оплатить или спросить
у неё, поднимать ли потолок.
Меряется страница **как она уезжает** — пустая; её записи растят её дальше, и
это её список, а не наша вёрстка.

Спрятанное проверяется **нажатием**, а не чтением разметки: `hidden` — атрибут,
который любой наш `display` в CSS перебивает молча. Ровно так форма один раз и
стояла развёрнутой во весь экран, считаясь спрятанной, — страница выросла на
292 точки. Теперь в стиле есть `[hidden]{display:none!important}`, а в линейке —
проба нажатием.

Осело меньше, чем кажется по карточке (было 1754): высоту ряда задаёт самая
длинная из четырёх — Киносаки с двумя сроками отмены, — а свернули мы у всех
поровну.

Высота документа ловит только то, что вылезло наружу; внутри карточки
содержимое переполняется молча, а страница остаётся ровно той же высоты —
поэтому каждая колонка меряется отдельно. Заодно проверяется, что ширина
отрезка в нитке это ночи (четыре ночи ровно вдвое шире двух), что на телефоне
ничего не торчит вбок и что по видимым целям можно попасть пальцем.

Проверки проверены падением, а не только успехом: возвращённый блёклый серый
даёт 180 просевших строк, возвращённая тёмная тема — ✗ на цвете фона,
развёрнутые по умолчанию подробности — ✗ на высоте и на семи проверках
свёртки. Каждый раз код выхода 1.

**Итог в долларах — сумма показанных долларов, а не пересчёт общей иены.** Из иены вышло бы $1 875, из четырёх чисел на экране — $1 876; когда
четыре числа в столбик не сходятся с итогом под ними, это первое, что бросается
в глаза. Расхождение в доллар — цена округления, в иенах всё точно.

## Дверь

`site/functions/` — замок, портированный с курса лекций (стоит перед живым
сайтом с 9 августа 2026). Печенье подписано паролем, живёт полгода, секрета в
себе не несёт.

**Дверь падает закрытой.** Без `JAPAN_PASSWORD` ответ на всё — 503. Отказ,
который мы можем себе позволить, — «Ни не может войти»; тот, который не
можем, — «её брони и суммы уехали в открытый доступ».

Пароль стоит на хосте как секрет Pages (`production` и `preview`), в
репозитории и в собранной папке его нет.

`deploy.sh` не выложит страницу, если:

- в `dist/` нет `functions/` или `_routes.json`;
- в выводе wrangler нет строки про Functions bundle (так вишлист однажды
  простоял три минуты нараспашку — wrangler берёт `functions/` из текущего
  каталога и молчит, если его там нет);
- дверь не держит на localhost: `wrangler pages dev` поднимается на той же
  связке функций, и её ломают — закрытая страница обязана дать 401, неверный
  пароль 401, верный открыть страницу. Это стоит **до** выкладки и не зависит
  ни от DNS, ни от сертификатов;
- боевая не отвечает 401 десять раз из десяти — включая **чистый адрес**, без
  `?knock=`: хвост в адресе уходит мимо кэша края и однажды уже спрятал
  удалённую страницу, отвечавшую 200 тридцать один час;
- правильный пароль не открывает страницу.

## Чего на странице нет и не будет

Номеров броней, PIN-кодов, карт, почты, ссылок в личный кабинет. Телефоны и
адреса отелей оставлены сознательно: это публичные данные гостиниц, и в дороге
они нужнее всего.

Страница не ходит ни на один чужой хост: стиль и скрипт внутри файла, шрифты
системные. Единственные внешние ссылки — карты Google по адресу отеля, и они
открываются только по нажатию. Это проверяется тестом: в дороге связь плохая.

## Известные шероховатости

- **У нового проекта Cloudflare несколько минут нет сертификата на вложенные
  поддомены** (`ветка.japan-2027-eti.pages.dev`). Из-за этого проверочная ветка
  как предохранитель не годилась: она падала бы с ошибкой TLS, а не с «двери
  нет», — и её заменила репетиция на localhost. Сам боевой адрес (один уровень)
  под общий сертификат `*.pages.dev` попадает и работал сразу.
- **Две вкладки, открытые разом, спорят за последнее слово.** Хранилище — одно
  значение целиком: правка читает его и кладёт обратно. Правка из вкладки,
  которая нажала раньше, потеряется. Для страницы, которую открывает один
  человек с одного телефона, это дешевле, чем пол-состояния из разных ключей.
- **KV догоняет себя не мгновенно.** Поэтому ответ на любую правку — всё
  состояние целиком, и страница рисует именно его, а не перечитывает список
  следом: перечитанное имеет шанс оказаться вчерашним.
- **Галочки и записи не в `trip.json`.** Что решено окончательно — переносить в
  файл, чтобы жило рядом с бронями и не зависело от хранилища.
- **`wrangler pages dev` оставляет за собой `workerd`**, который переживает
  SIGTERM, а node над ним поднимает его заново. `deploy.sh` это знает: идёт
  вверх по дереву до самого верхнего своего процесса и добивает, а на занятом
  порту отказывается стартовать вовсе — сервер прошлого прогона держит **старый**
  пароль и превращает репетицию в проверку не того, что собрали.

  Обход вверх по дереву при этом читал **не тот** родительский pid: имя
  процесса в `/proc/pid/stat` стоит в скобках и бывает с пробелами
  («npm exec wrangl»), поэтому `$4` — это «wrangl)», а не число. Обход
  останавливался на полпути, порт оставался занятым, и выкладка отказывалась
  стартовать на ровном месте. Читается то, что после последней скобки.
  Проверено вживую 23 августа: убитый `workerd` тут же родился заново, пока не
  снят самый верхний `node .../wrangler`.
- **`curl … | grep -q` под `set -o pipefail` падает оттого, что совпадение
  нашлось**: grep выходит на первом попадании, curl получает SIGPIPE. Все
  проверки сначала пишут ответ в файл. Это выглядело как мигающий замок.
