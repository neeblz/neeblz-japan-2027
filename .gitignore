# Рабочее хозяйство wrangler: сборки на лету и хранилища проверок
# (`round-state`, `hand-state`). Живёт ровно один прогон и в git ему не место —
# а `tmp/` из корневого .gitignore ловил только половину этого каталога.
.wrangler/
